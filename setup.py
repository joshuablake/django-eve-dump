from setuptools import setup, find_packages

setup(name = 'django-eve-dump',
      version = '0.1.0',
      description = 'Models for the EVE static DB',
      author = 'Joshua Blake',
      author_email = 'joshbblake@gmail.com',
      packages = find_packages(),
)
